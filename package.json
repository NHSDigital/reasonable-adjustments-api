{
  "name": "apim-definitions",
  "version": "0.0.1",
  "description": "OAS (Swagger v3) API Definition for Reasonable Adjustment Flag API",
  "scripts": {
    "lint": "node_modules/.bin/openapi-generator-cli validate -i specification/reasonable-adjustment-flag.yaml",
    "publish": "./node_modules/.bin/openapi-generator-cli generate -i specification/reasonable-adjustment-flag.yaml --generator-key v3.0  -g openapi -o build && rm openapitools.json && mv build/openapi.json build/reasonable-adjustment-flag.json",
    "check-licenses": "node_modules/.bin/license-checker --failOn GPL --failOn LGPL"
  },
  "author": "NHS Digital",
  "license": "(MIT OR OGL)",
  "homepage": "https://github.com/NHSDigital/reasonable-adjustments-api",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.6.0",
    "license-checker": "^25.0.1",
    "newman": "^5.3.2",
    "newman-reporter-html": "^1.0.5",
    "newman-reporter-htmlextra": "^1.12.1"
  }
}
