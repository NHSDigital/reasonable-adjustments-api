{
  "name": "apim-definitions",
  "version": "0.0.1",
  "description": "OAS (Swagger v3) API Definition for Reasonable Adjustment Flag API",
  "scripts": {
    "lint": "openapi-generator-cli validate -i specification/reasonable-adjustment-flag.yaml",
    "test": "npm run --prefix tests test:sandbox",
    "publish": "openapi-generator-cli generate -i specification/reasonable-adjustment-flag.yaml --generator-key v3.0 -g openapi -o build && rm openapitools.json && mv build/openapi.json build/reasonable-adjustment-flag.json",
    "check-licenses": "node_modules/.bin/license-checker --failOn GPL --failOn LGPL"
  },
  "author": "NHS Digital",
  "license": "(MIT OR OGL)",
  "homepage": "https://github.com/NHSDigital/reasonable-adjustments-api",
  "devDependencies": {
    "apigeetool": "^0.14.2",
    "license-checker": "^25.0.1",
    "newman": "^4.6.0",
    "newman-reporter-html": "^1.0.5",
    "newman-reporter-htmlextra": "^1.12.1",
    "node": "^13.12.0"
  },
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.1"
  }
}
